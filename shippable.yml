# Language setting
language: node_js
build_image : siddarthan/docker-build-nodejs
# Version number
node_js:
  - 0.12
build:
  ci:
    - ps -eax
    - ls -al


integrations:
    hub:
      - integrationName: siddarthan
        type: docker
        pull_only: true 
      
    
    
 
